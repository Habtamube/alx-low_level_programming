#include "main.h"

/**
 * main - a function that prints a string, followed by a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
    _puts_recursion("Puts with recursion");
    return (0);
}
